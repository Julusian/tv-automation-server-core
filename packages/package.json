{
  "private": true,
  "workspaces": {
    "packages": [
      "blueprints-integration",
      "server-core-integration"
    ],
    "nohoist": [
      "**/timeline-state-resolver-types"
    ]
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "watch": "concurrently --prefix \"[{name}]\" --names \"BI,SCI\" -c \"bgBlue.bold,bgGreen.bold\" \"yarn workspace tv-automation-sofie-blueprints-integration build:main --watch\" \"yarn workspace tv-automation-server-core-integration build:main --watch\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
